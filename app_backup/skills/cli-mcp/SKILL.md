---
name: cli-mcp
description: Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect the MCP server status, list registered tools and scopes, run tool invocations, and manage MCP audit logs from the CLI.

## Quick install

```bash
npm install -g nerve   # or: npx nerve
nerve --version
```

## Subcommands

### `mcp`

**Example:**

```bash
nerve mcp
```

### `mcp status`

Show MCP server status

**Flags:**

- `--json`

**Example:**

```bash
nerve mcp status
```

### `mcp restart`

Restart the MCP server

**Example:**

```bash
nerve mcp restart
```

### `mcp call <tool> [argsJson]`

**Flags:**

- `--args <json>`
- `--args-file <path>`
- `--stream`
- `--scope <s>`

**Example:**

```bash
nerve mcp call <tool> [argsJson]
```

### `mcp scopes`

**Flags:**

- `--tool <name>`

**Example:**

```bash
nerve mcp scopes
```

### `mcp tools`

**Example:**

```bash
nerve mcp tools
```

### `mcp list`

**Flags:**

- `--scope <s>`

**Example:**

```bash
nerve mcp list
```

### `mcp info <name>`

**Example:**

```bash
nerve mcp info <name>
```

### `mcp schema <name>`

**Flags:**

- `--io <kind>`

**Example:**

```bash
nerve mcp schema <name>
```

### `mcp audit`

**Example:**

```bash
nerve mcp audit
```

### `mcp tail`

**Flags:**

- `--follow`
- `--limit <n>`

**Example:**

```bash
nerve mcp tail
```

### `mcp stats`

**Flags:**

- `--period <p>`

**Example:**

```bash
nerve mcp stats
```
