---
name: cli-policy-audit
description: Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.

## Quick install

```bash
npm install -g nerve   # or: npx nerve
nerve --version
```

## Subcommands

### `audit`

**Example:**

```bash
nerve audit
```

### `audit tail`

**Flags:**

- `--source <s>`
- `--follow`
- `--limit <n>`

**Example:**

```bash
nerve audit tail
```

### `audit search <query>`

**Flags:**

- `--source <s>`
- `--since <ts>`
- `--until <ts>`
- `--limit <n>`
- `--actor <id>`
- `--action <a>`

**Example:**

```bash
nerve audit search <query>
```

### `audit export <file>`

**Flags:**

- `--source <s>`
- `--format <f>`
- `--since <ts>`
- `--until <ts>`

**Example:**

```bash
nerve audit export <file>
```

### `audit stats`

**Flags:**

- `--source <s>`
- `--period <p>`

**Example:**

```bash
nerve audit stats
```

### `audit get <id>`

**Flags:**

- `--source <s>`

**Example:**

```bash
nerve audit get <id>
```

### `logs`

**Flags:**

- `--follow`
- `--filter <level>`
- `--lines <n>`
- `--timeout <ms>`
- `--base-url <url>`
- `--request-id <id>`
- `--api-key <key>`
- `--combo <name>`
- `--status <code>`
- `--duration-min <ms>`
- `--duration-max <ms>`
- `--export <path>`

**Example:**

```bash
nerve logs
```

### `policy`

**Example:**

```bash
nerve policy
```

### `policy list`

**Flags:**

- `--kind <k>`
- `--scope <s>`

**Example:**

```bash
nerve policy list
```

### `policy get <id>`

**Example:**

```bash
nerve policy get <id>
```

### `policy create`

**Example:**

```bash
nerve policy create
```

### `policy update <id>`

**Example:**

```bash
nerve policy update <id>
```

### `policy delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve policy delete <id>
```

### `policy evaluate`

**Flags:**

- `--resource <r>`
- `--context <json>`

**Example:**

```bash
nerve policy evaluate
```

### `policy export <file>`

**Example:**

```bash
nerve policy export <file>
```

### `policy import <file>`

**Flags:**

- `--overwrite`

**Example:**

```bash
nerve policy import <file>
```

### `telemetry`

**Example:**

```bash
nerve telemetry
```

### `telemetry summary`

**Flags:**

- `--period <p>`
- `--compare-to <p>`

**Example:**

```bash
nerve telemetry summary
```

### `telemetry export`

**Flags:**

- `--out <path>`
- `--period <p>`

**Example:**

```bash
nerve telemetry export
```
