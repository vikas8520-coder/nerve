---
name: cli-contexts
description: Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.

## Quick install

```bash
npm install -g nerve   # or: npx nerve
nerve --version
```

## Subcommands

### `context-eng`

**Example:**

```bash
nerve context-eng
```

### `context-eng analytics`

**Flags:**

- `--period <p>`

**Example:**

```bash
nerve context-eng analytics
```

### `context-eng caveman`

**Example:**

```bash
nerve context-eng caveman
```

### `context-eng config`

**Example:**

```bash
nerve context-eng config
```

### `context-eng show`

**Example:**

```bash
nerve context-eng show
```

### `context-eng set`

**Flags:**

- `--aggressiveness <n>`
- `--max-shrink-pct <n>`
- `--preserve-tags <list>`

**Example:**

```bash
nerve context-eng set
```

### `context-eng rtk`

**Example:**

```bash
nerve context-eng rtk
```

### `context-eng config`

**Example:**

```bash
nerve context-eng config
```

### `context-eng show`

**Example:**

```bash
nerve context-eng show
```

### `context-eng set`

**Flags:**

- `--token-budget <n>`
- `--reserve-pct <n>`

**Example:**

```bash
nerve context-eng set
```

### `context-eng filters`

**Example:**

```bash
nerve context-eng filters
```

### `context-eng list`

**Example:**

```bash
nerve context-eng list
```

### `context-eng add`

**Flags:**

- `--priority <n>`
- `--action <a>`

**Example:**

```bash
nerve context-eng add
```

### `context-eng remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve context-eng remove <id>
```

### `context-eng test`

**Example:**

```bash
nerve context-eng test
```

### `context-eng raw-output <id>`

**Example:**

```bash
nerve context-eng raw-output <id>
```

### `context-eng combos`

**Example:**

```bash
nerve context-eng combos
```

### `context-eng list`

**Example:**

```bash
nerve context-eng list
```

### `context-eng get <id>`

**Example:**

```bash
nerve context-eng get <id>
```

### `context-eng assignments <id>`

**Example:**

```bash
nerve context-eng assignments <id>
```

### `contexts`

**Example:**

```bash
nerve contexts
```

### `contexts list`

List all contexts

**Example:**

```bash
nerve contexts list
```

### `contexts add <name>`

Add a new context

**Flags:**

- `--api-key <k>`
- `--api-key-stdin`
- `--access-token <t>`
- `--access-token-stdin`
- `--scope <s>`
- `--description <d>`

**Example:**

```bash
nerve contexts add <name>
```

### `contexts use <name>`

Switch active context

**Example:**

```bash
nerve contexts use <name>
```

### `contexts current`

Show the active context (server, auth, scope)

**Flags:**

- `--name-only`

**Example:**

```bash
nerve contexts current
```

### `contexts show <name>`

Show context details

**Example:**

```bash
nerve contexts show <name>
```

### `contexts remove <name>`

Remove a context

**Flags:**

- `--yes`

**Example:**

```bash
nerve contexts remove <name>
```

### `contexts rename <old> <new>`

Rename a context

**Example:**

```bash
nerve contexts rename <old> <new>
```

### `contexts export`

Export contexts to JSON

**Flags:**

- `--out <path>`
- `--no-secrets`

**Example:**

```bash
nerve contexts export
```

### `contexts import <file>`

Import contexts from a JSON file

**Flags:**

- `--merge`

**Example:**

```bash
nerve contexts import <file>
```

### `sessions`

**Example:**

```bash
nerve sessions
```

### `sessions list`

**Flags:**

- `--user <u>`
- `--kind <k>`
- `--active`
- `--limit <n>`

**Example:**

```bash
nerve sessions list
```

### `sessions show <sessionId>`

**Example:**

```bash
nerve sessions show <sessionId>
```

### `sessions expire <sessionId>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve sessions expire <sessionId>
```

### `sessions expire-all`

**Flags:**

- `--yes`

**Example:**

```bash
nerve sessions expire-all
```

### `sessions current`

**Example:**

```bash
nerve sessions current
```
