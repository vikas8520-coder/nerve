---
name: cli-plugins-skills
description: Manage Omni Skills (list, install, test, remove), plugins (create, configure), and persistent memory (search, add, clear) from the CLI.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage Omni Skills (list, install, test, remove), plugins (create, configure), and persistent memory (search, add, clear) from the CLI.

## Quick install

```bash
npm install -g nerve   # or: npx nerve
nerve --version
```

## Subcommands

### `memory`

**Example:**

```bash
nerve memory
```

### `memory search <query>`

**Flags:**

- `--type <type>`
- `--limit <n>`
- `--api-key <key>`
- `--token-budget <n>`

**Example:**

```bash
nerve memory search <query>
```

### `memory add`

**Flags:**

- `--content <text>`
- `--file <path>`
- `--type <type>`
- `--metadata <json>`
- `--api-key <key>`

**Example:**

```bash
nerve memory add
```

### `memory clear`

**Flags:**

- `--type <type>`
- `--older-than <duration>`
- `--api-key <key>`
- `--yes`

**Example:**

```bash
nerve memory clear
```

### `memory list`

**Flags:**

- `--type <type>`
- `--limit <n>`
- `--api-key <key>`

**Example:**

```bash
nerve memory list
```

### `memory get <id>`

**Example:**

```bash
nerve memory get <id>
```

### `memory delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve memory delete <id>
```

### `memory health`

**Example:**

```bash
nerve memory health
```

### `PLUGIN_NAME`

**Example:**

```bash
nerve PLUGIN_NAME
```

### `plugin plugin`

**Example:**

```bash
nerve plugin plugin
```

### `plugin list`

**Example:**

```bash
nerve plugin list
```

### `plugin install <name>`

**Flags:**

- `-y, --yes`

**Example:**

```bash
nerve plugin install <name>
```

### `plugin remove <name>`

**Flags:**

- `-y, --yes`

**Example:**

```bash
nerve plugin remove <name>
```

### `plugin info <name>`

**Example:**

```bash
nerve plugin info <name>
```

### `plugin search [query]`

**Example:**

```bash
nerve plugin search [query]
```

### `plugin update [name]`

**Example:**

```bash
nerve plugin update [name]
```

### `plugin scaffold <name>`

**Example:**

```bash
nerve plugin scaffold <name>
```

### `marketplace`

**Example:**

```bash
nerve marketplace
```

### `skills search [query]`

**Flags:**

- `--category <c>`
- `--tag <t>`
- `--limit <n>`
- `--sort <s>`

**Example:**

```bash
nerve skills search [query]
```

### `skills info <packageId>`

**Example:**

```bash
nerve skills info <packageId>
```

### `skills install <packageId>`

**Flags:**

- `--version <v>`
- `--enable`
- `--yes`

**Example:**

```bash
nerve skills install <packageId>
```

### `skills categories`

**Example:**

```bash
nerve skills categories
```

### `skills featured`

**Example:**

```bash
nerve skills featured
```

### `skills skills`

**Example:**

```bash
nerve skills skills
```

### `skills list`

**Flags:**

- `--type <type>`
- `--enabled`
- `--disabled`
- `--api-key <key>`

**Example:**

```bash
nerve skills list
```

### `skills get <id>`

**Example:**

```bash
nerve skills get <id>
```

### `skills install`

**Flags:**

- `--from-file <path>`
- `--from-url <url>`
- `--type <type>`
- `--enable`

**Example:**

```bash
nerve skills install
```

### `skills enable <id>`

**Example:**

```bash
nerve skills enable <id>
```

### `skills disable <id>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve skills disable <id>
```

### `skills delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve skills delete <id>
```

### `skills execute <id>`

**Flags:**

- `--input <json>`
- `--input-file <path>`
- `--timeout <ms>`

**Example:**

```bash
nerve skills execute <id>
```

### `skills executions`

**Flags:**

- `--skill <id>`
- `--limit <n>`
- `--status <s>`

**Example:**

```bash
nerve skills executions
```

### `skills skillssh`

**Example:**

```bash
nerve skills skillssh
```

### `skills list`

**Example:**

```bash
nerve skills list
```

### `skills install <url>`

**Example:**

```bash
nerve skills install <url>
```
