---
name: cli-cost-usage
description: View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.

## Quick install

```bash
npm install -g nerve   # or: npx nerve
nerve --version
```

## Subcommands

### `cost`

**Flags:**

- `--period <range>`
- `--since <date>`
- `--until <date>`
- `--group-by <field>`
- `--api-key <key>`
- `--limit <n>`

**Example:**

```bash
nerve cost
```

### `pricing`

**Example:**

```bash
nerve pricing
```

### `pricing sync`

**Flags:**

- `--provider <p>`
- `--force`

**Example:**

```bash
nerve pricing sync
```

### `pricing list`

**Flags:**

- `--provider <p>`
- `--model <m>`
- `--limit <n>`

**Example:**

```bash
nerve pricing list
```

### `pricing get <model>`

**Example:**

```bash
nerve pricing get <model>
```

### `pricing defaults`

**Example:**

```bash
nerve pricing defaults
```

### `pricing show`

**Example:**

```bash
nerve pricing show
```

### `pricing set`

**Flags:**

- `--input <p>`
- `--output <p>`
- `--cache-read <p>`
- `--cache-write <p>`

**Example:**

```bash
nerve pricing set
```

### `pricing diff`

**Flags:**

- `--model <m>`

**Example:**

```bash
nerve pricing diff
```

### `usage`

**Example:**

```bash
nerve usage
```

### `usage analytics`

**Flags:**

- `--period <range>`
- `--provider <id>`

**Example:**

```bash
nerve usage analytics
```

### `usage budget`

**Example:**

```bash
nerve usage budget
```

### `usage list`

**Example:**

```bash
nerve usage list
```

### `usage get [scope]`

**Example:**

```bash
nerve usage get [scope]
```

### `usage set <amount>`

**Flags:**

- `--scope <s>`
- `--period <p>`

**Example:**

```bash
nerve usage set <amount>
```

### `usage reset [scope]`

**Example:**

```bash
nerve usage reset [scope]
```

### `usage quota`

**Flags:**

- `--provider <id>`
- `--check`

**Example:**

```bash
nerve usage quota
```

### `usage logs`

**Flags:**

- `--limit <n>`
- `--search <q>`
- `--since <ts>`
- `--follow`
- `--api-key <k>`

**Example:**

```bash
nerve usage logs
```

### `usage utilization`

**Flags:**

- `--api-key <k>`

**Example:**

```bash
nerve usage utilization
```

### `usage history`

**Flags:**

- `--limit <n>`

**Example:**

```bash
nerve usage history
```

### `usage proxy-logs`

**Flags:**

- `--limit <n>`

**Example:**

```bash
nerve usage proxy-logs
```
