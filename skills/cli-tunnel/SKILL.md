---
name: cli-tunnel
description: Start and stop tunnel connections (ngrok, Cloudflare, custom) from the CLI. Inspect active tunnel URLs, configure authentication, and test external reachability.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Start and stop tunnel connections (ngrok, Cloudflare, custom) from the CLI. Inspect active tunnel URLs, configure authentication, and test external reachability.

## Quick install

```bash
npm install -g nerve   # or: npx nerve
nerve --version
```

## Subcommands

### `tunnel`

**Example:**

```bash
nerve tunnel
```

### `tunnel list`

**Flags:**

- `--json`

**Example:**

```bash
nerve tunnel list
```

### `tunnel create [type]`

**Example:**

```bash
nerve tunnel create [type]
```

### `tunnel stop <type>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve tunnel stop <type>
```

### `tunnel status <type>`

**Flags:**

- `--json`

**Example:**

```bash
nerve tunnel status <type>
```

### `tunnel logs <type>`

**Flags:**

- `--tail <n>`

**Example:**

```bash
nerve tunnel logs <type>
```

### `tunnel info <type>`

**Flags:**

- `--json`

**Example:**

```bash
nerve tunnel info <type>
```

### `tunnel rotate <type>`

**Flags:**

- `--yes`

**Example:**

```bash
nerve tunnel rotate <type>
```
